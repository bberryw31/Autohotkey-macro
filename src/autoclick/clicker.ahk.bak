CoordMode, Mouse, Window

Alt::
{
	looping := true
    while looping {
        if GetKeyState("LCtrl") {
            looping := false
        }
		;Loopww
		{
			random, int, 80, 120
			sleep %int%
			send {RButton}
		}
	}
}

return
